/**
 * Meta route representation
 */
export interface IAppConfig {
    /** Meta route the router uses */
    metaRoute: string;

    /** Entry point of the routed client app */
    baseUrl: string;
}
